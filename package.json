{
  "name": "report-downloader-bun",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && bun install && bun run build && cd .. && rm -rf public && cp -r frontend/dist public",
    "dev": "bun --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@vercel/node": "^5.1.0"
  }
}
